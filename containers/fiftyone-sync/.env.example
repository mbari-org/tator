# Copy to .env and adjust. Used when running the fiftyone-sync API (e.g. uvicorn)
# so it connects to the MongoDB started by this compose stack.
#
# When the API runs on the host:
FIFTYONE_DATABASE_URI=mongodb://localhost:27017
#
# When the API runs in a container on the same Docker network as mongo:
# FIFTYONE_DATABASE_URI=mongodb://mongo:27017
#
# Optional: Fast-VSS embedding service base URL (no trailing slash)
# FASTVSS_API_URL=http://localhost:8000
#
# Optional: Redis for queued sync (e.g. REDIS_HOST=redis if using Tator compose network)
# REDIS_HOST=
# REDIS_PORT=6379
